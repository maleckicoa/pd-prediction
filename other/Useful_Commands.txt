WIPE OUT EVERYTHING (Including MLflow and Postgres VOlumes)

cd demo-service/pd-prediction
docker compose --profile fetch down -v --remove-orphans
docker compose --profile fetch up -d --build


WIPE OUT EVERYTHING EXCEPT MLFlow volumes 

cd demo-service/pd-prediction
docker compose --profile fetch down --remove-orphans
docker volume rm pd-prediction_pgdata
docker compose --profile fetch up -d --build